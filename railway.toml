# Untuk railway.toml
[build]
builder = "nixpacks"
buildCommand = "npm install && npm run build && php artisan optimize"

# Jika Anda menggunakan Dockerfile, update perintah RUN:
RUN npm install && NODE_OPTIONS='--experimental-json-modules' npm run build && php artisan optimize